<div class="clearfix"></div><br/><br/>
<div class="col-xs-12 col-md-4 col-md-offset-4">
    <div class="text-center"><img src="{{ asset("img/logo-blue.png") }}" /></div>
    <h1 class="heading text-center"></h1>
    <p class="heading-desc text-center">Reset your password using the form below!</p>
    <form class="form-signin" action="{{ path("fos_user_resetting_send_email") }}" method="post">
        {% if error is defined and error %}
            <div class="alert alert-danger">The follow errors just happened:<br>
                - {{ error|trans({}, 'FOSUserBundle') }}
            </div>
        {% endif %}
        <label for="username">{{ 'resetting.request.username'|trans({}, 'FOSUserBundle') }}</label>
        <input type="email" id="username" name="username" required="required" placeholder="Type your Email" class="form-control"/>
        <br/>
        <button class="btn btn-sm btn-primary btn-block" type="submit">{{ 'resetting.request.submit'|trans({}, 'FOSUserBundle') }}</button>
    </form>
    <a href="{{ path('hwi_oauth_connect') }}" class="text-center account-creation pull-left">Have an account already? Login</a>
    <a href="{{ path("fos_user_registration_register") }}" class="pull-right help">Create an account</a>
</div>